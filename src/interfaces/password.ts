export interface Password {
  passwordName: string;
  password: string;
  passwordUser?: string;
}

export interface PasswordRegistration {
  passwordName: string;
  passwordValue: string;
  passwordUser?: string;
}
